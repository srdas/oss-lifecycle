[project]
name = "oss-lifecycle"
version = "0.1.0"
description = "A package to analyze open-source GitHub projects life cycles"
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [
    { name = "Sanjiv Das", email = "srdas@scu.edu" }
]
requires-python = ">=3.12"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dependencies = [
    "flask", 
    "flask-cors",
    "pandas",
    "numpy",
    "matplotlib",
    "scikit-learn",
    "scipy",
    "GitPython",
]

[project.urls]
Homepage = "https://github.com/srdas/oss-lifecycle/tree/main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
packages = ["src", "data", "docs", "images", "LICENSE", "paper.bib", "paper.md", "paper.pdf", "README.md", "requirements.txt", "pyproject.toml"]
